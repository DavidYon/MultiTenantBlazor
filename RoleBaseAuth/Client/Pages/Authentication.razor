@page "/authentication/{action}"
@using Microsoft.AspNetCore.Components.WebAssembly.Authentication
@inject Tenant tenant
@inject NavigationManager nav
<RemoteAuthenticatorView Action="@Action" @ref="rav" />

@code{
    [Parameter] public string Action { get; set; }
    RemoteAuthenticatorView rav;
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            //rav.ApplicationPaths.LogInCallbackPath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.LoginCallbackPath;
            //rav.ApplicationPaths.LogInPath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.LoginPath;
            //rav.ApplicationPaths.LogInFailedPath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.LoginFailedPath;
            //rav.ApplicationPaths.LogOutCallbackPath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.LogoutCallbackPath;
            //rav.ApplicationPaths.LogOutFailedPath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.LogoutFailedPath;
            //rav.ApplicationPaths.LogOutPath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.LogoutPath;
            //rav.ApplicationPaths.LogOutSucceededPath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.LogoutSucceededPath;
            //rav.ApplicationPaths.ProfilePath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.ProfilePath;
            //rav.ApplicationPaths.RegisterPath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.RegisterPath;
            //rav.ApplicationPaths.RemoteProfilePath = tenant.Identifier + "/" + RemoteAuthenticationDefaults.RemoteProfilePath;
            //rav.ApplicationPaths.RemoteRegisterPath = tenant.Identifier + "/" + rav.ApplicationPaths.RemoteRegisterPath;

            //rav.AuthenticationState.ReturnUrl = nav.BaseUri + tenant.Identifier;
        }
    }
}
